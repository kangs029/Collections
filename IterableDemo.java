/*
Iterable interface has iterator() method, using which we can iterate over collection
Collection extends Iterable interface
Since Collection implements iterable and has their interator() method implementaed, so we use for Each loop
*/
public class IterableDemo {
    
    
}
